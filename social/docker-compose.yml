version: "3.9"

networks:
  postgresql:
    driver: bridge

services:
  # PostgreSQL database
  postgresql:
    image: postgres:17.6 # Версия PostgreSQL
    container_name: 'postgresql-soc'
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${SOC_POSTGRES_DATABASE}
      POSTGRES_USER: ${SOC_POSTGRES_USER}
      POSTGRES_PASSWORD: ${SOC_POSTGRES_PASSWORD}
      PGDATA: "/var/lib/postgresql/data/pgdata"
      POSTGRES_HOST_AUTH_METHOD: trust
    volumes:
      - ./config/postgres.conf:/etc/postgresql/postgresql.conf # конфиг БД
      - ./docker/postgresql/data:/var/lib/postgresql/data # том для того, чтобы при перезапуске контейнера все данные сохранялись
    command: postgres -c config_file=/etc/postgresql/postgresql.conf
    ports:
      - 5433:5432
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${SOC_POSTGRES_USER} -d ${SOC_POSTGRES_DATABASE}" ]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - postgresql