version: "3.9"

networks:
  postgresql:
    driver: bridge

services:
  # PostgreSQL database
  postgresql:
    image: postgres:17.6 # Версия PostgreSQL
    container_name: 'postgresql-chat'
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${CHAT_POSTGRES_DATABASE}
      POSTGRES_USER: ${CHAT_POSTGRES_USER}
      POSTGRES_PASSWORD: ${CHAT_POSTGRES_PASSWORD}
      PGDATA: "/var/lib/postgresql/data/pgdata"
      POSTGRES_HOST_AUTH_METHOD: trust
    volumes:
      - ./config/postgres.conf:/etc/postgresql/postgresql.conf # конфиг БД
      - ./docker/postgresql/data:/var/lib/postgresql/data # том для того, чтобы при перезапуске контейнера все данные сохранялись
    command: postgres -c config_file=/etc/postgresql/postgresql.conf
    ports:
      - 5432:5432
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${CHAT_POSTGRES_USER} -d ${CHAT_POSTGRES_DATABASE}" ]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - postgresql